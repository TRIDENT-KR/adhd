> 이 문서는 Claude Code(Fable 5)가 코드베이스를 직접 읽고 상세 구현 기획안을 작성할 때 참고하는 **소스 오브 트루스** 명세서입니다. 회색으로 표시된 "확인 필요" 항목은 Fable5가 코드베이스를 읽은 뒤에도 답이 안 나오면 반드시 사용자에게 질문해야 합니다.
> 

---

## 0. 문서 사용 방법

1. 이 명세서 + GitHub 레포(TRIDENT-KR/adhd) 코드베이스 전체를 함께 읽는다.
2. 현재 코드 구현 상태와 이 명세서의 목표 상태를 비교해 **차이(gap)** 를 찾는다.
3. 명세서에 "확인 필요"로 표시된 항목, 그리고 코드를 읽어도 판단이 안 서는 지점은 목록화해서 사용자와 인터뷰한다.
4. 인터뷰 결과를 반영해 `MORA_IMPLEMENTATION_PLAN.md` (상세 구현 기획안)를 작성한다. 이 단계에서는 실제 코드 구현은 하지 않는다.

---

## 1. 앱 개요

- **앱 이름**: Mora (모라)
- **부제**: Voice-First Task Manager / 음성 AI 루틴 & 할 일 관리
- **한 줄 정의**: "마이크 하나로 모든 일정과 루틴을 기록하는, ADHD 친화적 극강 미니멀 통합 관리 앱"
- **플랫폼**: Native iOS (SwiftUI), 현재 iOS 전용

### 배경 / 페인 포인트

기존 알람·미리알림·캘린더 앱은 기능이 파편화되어 있고, 텍스트 입력 자체가 인지적 장벽이다. ADHD 성향 사용자는 앱을 열고 → 카테고리를 선택하고 → 타이핑하는 3단계 마찰 때문에 기록을 포기하고 중요한 일정을 반복적으로 놓친다. Mora는 **"말하면 끝"** 이라는 단일 인터랙션으로 이 마찰을 없앤다.

### 타겟 사용자

- **주 타겟**: ADHD 진단/성향이 있는 25~35세 직장인. 생산성 앱을 여러 번 시도했다 실패한 경험 있음. 복잡한 UI·설정 화면에 압도감을 느낌. 타이핑보다 말하기를 압도적으로 선호. iPhone 사용자.
- **보조 타겟**: ADHD 진단 없이도 정보 과잉으로 집중력 유지가 어려운 사람, 미니멀 UX 선호자.

### 해결하는 문제 (JTBD)

1. 루틴 기록 — "매일 아침 약 먹기" 같은 반복 루틴을 말 한마디로 등록
2. 일정 등록 — "다음 주 화요일 3시 치과" 같은 약속을 음성으로 캘린더에 저장
3. 빠른 메모 — 생각이 스칠 때 즉시 말해서 저장
4. 주간 조망 — 이번 주 굵직한 약속을 한눈에 파악 (월간 캘린더 X, 7일 뷰 O)

---

## 2. 정보 구조 & 탭별 기능 명세

### Tab 1 — Home (Voice)

**목적**: 음성 입력 전용 랜딩 화면. 화면 중앙에 커다란 마이크 버튼 하나만 존재.

확인 필요:

- 마이크 버튼 인터랙션 방식 (탭하면 시작/정지 토글 vs 길게 누르는 동안만 녹음)
- 리스닝/처리/완료/오류 각 상태별 Voice Pulse 애니메이션 및 색상 전환 스펙 (코드에서 현재 구현 상태 확인)
- 인식된 텍스트를 저장 전에 사용자에게 보여주고 확인/수정하게 할지, 바로 저장할지
- 파싱 실패(모호한 발화, 카테고리 판별 불가) 시 재시도 UX
- 언어 자동 감지 vs 수동 전환 (한/영/일 3개 국어 지원)
- 저장 완료 후 화면 전환 여부 (Home에 머무를지, 해당 탭으로 이동할지)

### Tab 2 — Routine

**목적**: 매일 반복되는 루틴 + 오늘의 할 일 관리.

확인 필요:

- 루틴 생성/편집/삭제를 음성 외에 수동으로도 할 수 있는지, 가능하다면 그 UI
- 완료 체크 인터랙션 (스와이프, 탭, 체크박스 등)과 완료 시 피드백(햅틱/애니메이션)
- 루틴 순서 재배열 가능 여부
- 스트릭(연속 달성일) 등 동기부여 요소 포함 여부
- 루틴별 알림/리마인더 설정 가능 여부 및 시점
- "오늘의 할 일"과 "반복 루틴"이 UI상 같은 리스트인지 별도 섹션인지

### Tab 3 — Planner

**목적**: 이번 주 7일 주간 뷰 + 굵직한 약속 목록. 월간 캘린더는 금지.

확인 필요:

- 주간 뷰 탐색 방식 (가로 스와이프, 좌우 화살표 등)과 오늘 날짜 기본 위치
- 이벤트 카드에 표시할 정보 밀도 (제목/시간만 vs 장소/메모 포함)
- iOS EventKit(캘린더 앱) 연동 여부 — 양방향 동기화인지 Mora 자체 저장소인지
- 일정 알림/리마인더 발송 시점(예: 30분 전 등) 설정 가능 여부
- 일정 수동 추가/편집 UI 존재 여부

---

## 3. 음성 인식 & AI 처리 파이프라인

- 현재: Cloud LLM으로 음성 → 텍스트 → 의도/데이터 파싱
- 향후 로드맵: On-device SLM (Apple Neural Engine NPU 활용) 전환
- 3개 국어(한국어/영어/일본어) 지원

확인 필요:

- 의도 분류 체계 — "루틴"/"일정"/"메모" 중 어떤 것으로 저장할지 판별하는 기준과 프롬프트/로직
- 애매한 발화(예: 카테고리가 불분명한 경우) 처리 방식 — 기본값 지정? 사용자에게 재확인?
- LLM 호출 실패/네트워크 오류 시 폴백 동작
- On-device SLM 전환 시점의 트리거 조건 및 우선순위 (이번 구현 범위에 포함되는지 여부)

---

## 4. 데이터 모델 (초안 — 코드베이스 확인 후 확정 필요)

확인 필요: Supabase 스키마 상 아래 엔터티가 이미 존재하는지, 필드가 무엇인지 코드에서 직접 확인.

- Routine (반복 루틴): 제목, 반복 주기, 시간, 완료 상태, 생성일 등
- Task (오늘의 할 일): 제목, 완료 상태, 관련 루틴 여부
- Event/Schedule (일정): 제목, 날짜/시간, 장소, 메모, 알림 시점
- Memo (빠른 메모): 텍스트, 생성 시각, 태그/카테고리 여부
- User/Auth: 로그인 방식 (Apple 로그인 등), 온보딩 완료 여부

---

## 5. 위젯 (iOS Widget Extension)

확인 필요:

- 위젯 종류 (Home Screen / Lock Screen) 및 크기 (Small/Medium/Large)
- 위젯에 표시할 정보 (오늘 루틴 진행률, 다음 일정, 빠른 음성 입력 딥링크 등)
- 위젯에서 마이크 딥링크로 앱 실행 가능하게 할지

---

## 6. 알림 / 리마인더

확인 필요:

- 로컬 알림(UNUserNotificationCenter) vs 푸시 알림 사용 여부
- 루틴/일정별 알림 커스터마이징 범위
- 알림 권한 요청 시점 (온보딩 중 vs 최초 기능 사용 시)

---

## 7. 디자인 시스템

### Creative North Star

**"The Guided Sanctuary"** — 단순 도구가 아니라 진입 순간 심리적 안도감을 주는 조용한 성소. 모든 요소는 불안을 낮추고 단 하나의 행동에만 집중하게 유도.

### 컬러 팔레트

| 역할 | 이름 | Light Mode | Dark Mode | 사용처 |
| --- | --- | --- | --- | --- |
| 배경 | Background | `#F9F9F7` | `#1A1A1A` | 앱 전체 배경 (따뜻한 종이 화이트) |
| 포인트 | Primary | `#934A2E` | `#FFB59B` | CTA 버튼, 핵심 강조 요소 (테라코타) |
| 포인트 컨테이너 | Primary Container | `#D27C5C` | `#7C3F24` | 그라디언트 끝점 |
| 성공/피드백 | Tertiary | `#006A63` | `#4FDBD1` | 완료 상태, 음성 인식 피드백 |
| 보조 텍스트 | OnSurfaceVariant | `#54433D` | `#CFC0B8` | 서브 텍스트, 아이콘, 비활성 상태 |
| 섹션 배경 | SurfaceContainerLow | `#F4F4F2` | `#252525` | 카드 배경, 섹션 구분 |

⚠️ 절대 금지: `#000000` 또는 순수 중성 회색. 반드시 따뜻한 톤의 `OnSurface(#1A1C1B)` 계열 사용.

### 타이포그래피 (듀얼 폰트 시스템)

| 폰트 | 역할 | 적용 규칙 |
| --- | --- | --- |
| Manrope | Display / Headline | `display-lg(3.5rem)`, `display-md` → letter-spacing: -2% |
| Inter | Body / Label | `body-lg`, `body-md` → letter-spacing: +0.02em |

계층 규칙: 현재 루틴 스텝은 `display-lg(34px, semibold)`, 나머지는 `title-sm` 또는 `body-md`. 한 화면에 "포커스 포인트"는 단 하나여야 함.

### 레이아웃 철학 — Organic Asymmetry

- 그리드 박스의 균일 나열 거부. 헤더 좌측 정렬 + 주요 CTA 우측 배치
- 초광폭 여백: `spacing-12(4rem)` 이상을 컴포넌트 간 적극 사용
- "텍스트 벽" 금지 — 직관적 아이콘으로 대체

### The No-Line Rule

- 1px solid border로 섹션 구분 절대 금지
- 구분은 (1) `SurfaceContainerLow` 배경색 전환, (2) `spacing-10/12/16` 네거티브 스페이스로만 처리

### 핵심 컴포넌트 (코드에서 현재 구현 상태 직접 확인 필요)

- Voice Pulse (핵심 아이덴티티 컴포넌트)
- 버튼 시스템
- 카드 & 리스트
- Glass Effect (프리미엄 감성)
- 햅틱 피드백

> 위 5개 컴포넌트는 명세서 원본에 세부 스펙이 비어 있음. `DesignSystem.swift`, `HomeVoiceInterfaceView.swift`를 읽고 현재 구현된 스펙(애니메이션 타이밍, 색상, 크기)을 그대로 문서화하거나, 구현이 없다면 이 문서의 톤앤매너 규칙에 맞춰 신규 스펙을 제안할 것.
> 

### 디자인 금기 사항 (Don'ts)

1. 하드 그레이 사용 금지 — `#000000`이나 중성 회색 대신 `on-surface(#1A1C1B)` 계열
2. 1px 구분선 금지 — 수직 여백(`spacing-12`)으로 대체
3. 정보 밀도 초과 금지 — 단일 화면 실행 가능 액션 3개 이상 금지 (초과 시 Progressive Disclosure)
4. 월간 캘린더 사용 금지 — 플래너 탭은 오늘 기준 7일(주간 가로 스크롤)만
5. 텍스트 장벽 금지 — 아이콘 + 짧은 레이블 조합

### 디자인 지침 (Do's)

1. 여백을 기능으로 인식 — 문장 하나 + 버튼 하나만 있어도 완성된 화면
2. 의도적 비대칭 — 헤더 좌측 정렬 + 주요 CTA 우측 배치
3. 부드러운 전환 — 모든 상태 변화는 `300ms` 이상, `cubic-bezier(0.4, 0, 0.2, 1)` easing
4. Shadow = Glow — `OnSurfaceVariant(#54433D)` 5% opacity, 40px blur, 10px Y-offset
5. 배경 전환으로 구조화 — `surface(#F9F9F7)` 위 `surfaceContainerLow(#F4F4F2)` 레이어

---

## 8. 기술 스택

- **플랫폼**: Native iOS (SwiftUI)
- **백엔드**: Supabase
- **AI 처리**: Cloud LLM (현재) → On-device SLM (Apple Neural Engine NPU) 전환 예정
- **위젯**: iOS Widget Extension 지원

---

## 9. 비기능 요구사항 (확인 필요)

- 오프라인 지원 범위 (음성 입력이 오프라인에서 동작해야 하는지)
- 데이터 동기화 정책 (Supabase realtime 사용 여부, 멀티 디바이스 지원 여부)
- 접근성 (VoiceOver, Dynamic Type 지원 범위)
- 성능 목표 (음성 인식 응답 지연 허용치)
- 온보딩 흐름 (권한 요청 순서: 마이크, 알림, 캘린더 등)
- 로그인/인증 방식 (Apple 로그인, 이메일 등)
- 인앱 결제/구독 여부 및 유료 기능 경계
- 다국어 UI 전환 방식 (시스템 언어 자동 감지 vs 앱 내 설정)

---

## 10. 참고 자료 (원본 소스)

- GitHub Repository: https://github.com/TRIDENT-KR/adhd
- README.md (기획 문서): https://github.com/TRIDENT-KR/adhd/blob/main/README.md
- DESIGN.md (디자인 시스템 원칙): https://github.com/TRIDENT-KR/adhd/blob/main/DESIGN.md
- DesignSystem.swift (컬러/컴포넌트 코드): https://github.com/TRIDENT-KR/adhd/blob/main/ADHD/DesignSystem.swift
- HomeVoiceInterfaceView.swift (Voice Pulse 구현): https://github.com/TRIDENT-KR/adhd/blob/main/ADHD/HomeVoiceInterfaceView.swift

---

## 11. 확인 필요 항목 종합 (Open Questions Log)

> Fable5는 코드베이스를 읽은 후에도 아래 항목에 대한 답이 명확하지 않으면, 반드시 이 목록을 기반으로 사용자에게 구조화된 질문을 던져 인터뷰를 진행해야 한다.
> 
1. Home 탭: 마이크 인터랙션 방식(탭 토글 vs 홀드), 인식 결과 확인 UI 필요 여부, 파싱 실패 시 재시도 흐름
2. Routine 탭: 수동 CRUD UI 필요 여부, 완료 인터랙션, 스트릭 기능 포함 여부, 알림 커스터마이징 범위
3. Planner 탭: EventKit 연동 여부, 이벤트 카드 정보 밀도, 수동 추가/편집 UI 여부
4. 음성 파싱: 의도 분류 로직/프롬프트, 애매한 발화 처리, LLM 실패 폴백
5. 데이터 모델: Supabase 스키마 확정 (Routine/Task/Event/Memo/User)
6. 위젯: 종류/크기, 표시 정보, 딥링크 여부
7. 알림: 로컬 vs 푸시, 커스터마이징 범위, 권한 요청 시점
8. 비기능: 오프라인 범위, 동기화 정책, 접근성, 성능 목표, 온보딩 흐름, 로그인 방식, 결제 여부, 다국어 전환 방식
9. 핵심 컴포넌트(Voice Pulse, 버튼, 카드, Glass Effect, 햅틱) 세부 스펙 — 코드 우선 확인, 불명확 시 질문

---

*최초 작성: Notion AI · 2026-07-07 · 원본: MORA 기획 문서 기반*
